<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cy.mapper.SessionMapper">

    <select id="pageList" resultType="com.cy.entity.Session">
        SELECT id,user_id,meal_type,total_calories,protein,fat,carbs,status,create_time
        FROM session
        WHERE user_id = #{userId}

        <if test="begin != null">
            AND create_time &gt;= #{begin}
        </if>

        <if test="end != null">
            AND create_time &lt;= #{end}
        </if>

        ORDER BY create_time DESC
    </select>

</mapper>